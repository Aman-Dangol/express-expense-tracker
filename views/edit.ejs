<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/public/css/style.css" />
    <title>Document</title>
  </head>
  <body>
    this is edit page <%= data[0].category %>
    <main>
      <section>
        <form action="/update" method="post">
          <input type="text" name="id" value="<%= data[0].id  %>" hidden />
          <label for="title">Title</label>
          <input type="text" name="title" value="<%= data[0].title  %>" />
          <label for="category">Category</label>
          <select name="category">
            <option value="food"  <% if (data[0].category == "food") { %>
              selected       
              <% } %>>food</option>
            <option value="drink"  <% if (data[0].category == "drink") { %>
              selected       
              <% } %>>drink</option>
            <option value="sweets"  <% if (data[0].category == "sweets") { %>
              selected       
              <% } %>>sweets</option>
            <option value="fruits"  <% if (data[0].category == "fruits") { %>
              selected       
              <% } %>>fruits</option>
          </select>
          <label for="amount">Amount</label>
          <input type="text" name="amount" value="<%= data[0].amount %>" />
          <button>Submit</button>
        </form>
      </section>
    </main>
  </body>
</html>
